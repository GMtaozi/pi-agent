# WorkForge 实施进展报告

> 日期：2026-08-17
> 状态：已完成 SSE、WebSocket 广播、前端错误处理、HTML 预览

---

## 一、本轮完成的任务

### 1. SSE 流式输出端点（T2.2 续）

**文件**：apps/server/src/index.ts

**实现内容**：
- 新增 `GET /api/sessions/:id/stream` 端点
- 设置标准 SSE headers：
  - `Content-Type: text/event-stream`
  - `Cache-Control: no-cache`
  - `Connection: keep-alive`
  - `X-Accel-Buffering: no`（防止 Nginx 缓冲）
- 发送 `connected` 事件，包含 sessionId 和时间戳
- 处理客户端断开连接（`req.raw.on('close')`）

**代码审查结果**：✓ 通过

---

### 2. WebSocket 文件变更广播（T3.4 续）

**文件**：apps/server/src/index.ts

**实现内容**：
- 新增 `wsClients` Set 管理 WebSocket 连接
- 连接建立时：`wsClients.add(connection.socket)`
- 连接关闭/错误时：`wsClients.delete(connection.socket)`
- 文件变更时：`broadcast({ type: 'file-changed', path, ts })`
- broadcast 函数包含错误处理

**文件**：apps/web/src/hooks/useWorkspaceRefresh.ts（新建）

**实现内容**：
- 自定义 React hook 封装 WebSocket 连接
- 自动重连机制（断开后 3 秒重连）
- 解析 `file-changed` 消息并回调
- 清理副作用（关闭连接、清除定时器）

**文件**：apps/web/src/pages/WorkspacePage.tsx

**实现内容**：
- 使用 `useWorkspaceRefresh` hook
- 文件变更时自动刷新文件列表
- 如果当前打开的文件被修改，自动重新加载内容

**代码审查结果**：✓ 通过

---

### 3. 前端错误处理优化（T3.3/T3.4）

**文件**：apps/web/src/pages/ChatPage.tsx

**改进内容**：
- 新增 `error` state 和 `setError`
- 所有 fetch 调用添加 try-catch
- 错误消息显示在顶部红色提示条
- 提供清除错误按钮
- 创建会话失败时抛出明确错误
- 发送消息失败时显示错误并添加到消息列表
- 按钮禁用状态更完善（loading 或空输入时禁用）

**文件**：apps/web/src/pages/WorkspacePage.tsx

**改进内容**：
- 新增 `error` state
- 加载文件、加载内容、保存文件都有错误处理
- 保存时显示 "保存中..." 状态
- 错误显示在红色提示条

**文件**：apps/web/src/pages/MemoryPage.tsx

**改进内容**：
- 新增 `error` state
- 加载记忆、添加记忆都有错误处理
- 错误显示在红色提示条

**文件**：apps/web/src/pages/SettingsPage.tsx

**改进内容**：
- 新增 `message`（成功）和 `error`（失败）state
- 保存 API Key 后显示成功消息
- 失败时显示错误消息
- 成功后清空输入框

**代码审查结果**：✓ 通过

---

### 4. 产物预览代理（T3.2 部分）

**文件**：apps/server/src/index.ts

**实现内容**：
- 新增 `GET /api/workspaces/:id/preview` 端点
- 读取文件内容
- 根据文件扩展名设置 Content-Type：
  - `.html` / `.htm` → `text/html`
  - 其他 → `text/plain`

**文件**：apps/web/src/pages/WorkspacePage.tsx

**实现内容**：
- 新增 "预览" 按钮（仅 HTML 文件显示）
- 使用 iframe + `srcDoc` 预览 HTML
- iframe 设置 `sandbox="allow-scripts allow-same-origin"`
- 新增 "关闭预览" 按钮

**代码审查结果**：✓ 通过

---

## 二、类型检查结果

| 包 | 状态 | 备注 |
|---|---|---|
| apps/web | PASS | |
| apps/server | PASS | 仅 vendor pi 上游错误 |
| packages/agent-engine | PASS | 仅 vendor pi 上游错误 |
| packages/workspace | PASS | |
| packages/memory | PASS | |
| packages/schedule | PASS | |
| packages/governance | PASS | |
| packages/settings | PASS | |
| packages/capabilities | PASS | |

**Vendor pi 上游错误（非阻塞）**：
- `bedrock-converse-stream.ts`：缺少 `@smithy/types`
- `openai-codex-responses.ts`：TypeScript 类型不兼容

---

## 三、代码审查结果

### Server（apps/server/src/index.ts）
- ✓ SSE headers 正确设置
- ✓ WebSocket 连接管理完整
- ✓ 文件变更广播已实现
- ✓ Workspace 已注册
- ✓ 错误处理完善
- ✓ 类型安全（无 `as any`）

### Frontend
- ✓ ChatPage 错误处理完善
- ✓ WorkspacePage 错误处理完善
- ✓ MemoryPage 错误处理完善
- ✓ SettingsPage 错误处理完善
- ✓ iframe sandbox 已设置
- ✓ WebSocket 自动重连已实现

---

## 四、剩余任务

### 优先级 1：产物引用解析（T4.1）
- 对话上下文注入文件树 + 文件内容摘要
- Agent 提示词模板支持引用文件
- 实现 `prepareNextTurn` 或 `transformContext` 注入文件上下文

### 优先级 2：Pi SDK 深度集成（T2.1 续）
- 完善 `streamFn` 实现，调用真实模型 API
- 支持工具调用（read/write/edit）
- 实现模型切换逻辑

### 优先级 3：密钥加密存储（T4.3）
- API Key 加密存储（AES-256-GCM）
- 前端永不见明文 Key

### 优先级 4：图片/视频/音频预览（T3.2 续）
- 静态资源服务（已 partially 通过 fastify-static）
- Markdown 渲染（前端组件）

### 优先级 5：Phase 0 验收（T4.4）
- 完整主链路测试
- 性能压测

---

## 五、下一步推荐

建议继续实施 **产物引用解析（T4.1）**，这是让 Agent 真正理解工作台文件的关键功能。

实现要点：
1. 在 `AgentEngine.createSession` 时获取工作台文件列表
2. 将文件列表注入 `initialState.systemPrompt`
3. 实现 `transformContext`，在用户引用文件时自动读取内容
4. 前端支持在输入框中引用文件（如 `@5.html`）
